# Auto-retro: 2026-05-11 15:26
Scope: project (PiPanything)
Retros reviewed: 3

## Deferred for human review

- **"Applied:" tag in the `/retros:retro` skill** — when the retro skill applies a queued CLAUDE.md/BACKLOG edit, append a tiny `Applied: <YYYY-MM-DD>` marker to the originating retro's "Suggested unblock" or "queued edit" bullet so future reviewers don't re-derive whether the change already landed. *Diagnosis:* single load-bearing cause confirmed in retro 1 (reviewer had to open CLAUDE.md to verify the edit was already there). *Why deferred:* skill modification (`~/.claude/skills/retros/retro.md` or the plugin equivalent) — outside the safe set.
  - Source retro(s): `archive/2026-05-11-130929-retro-review-pipanything.md`
  - Suggested change shape:
    ```
    In retros/retro.md "Apply" step, when persisting an edit that resolved a
    bullet referenced in the originating retro, append:
        Applied: 2026-05-11 (commit <sha-or-where>)
    to that bullet *in place* before moving the retro into archive/.
    ```

- **Plan-mode-flatten convention for the `/retros:retro` skill** — when invoked under plan mode, collapse the normally-iterative diagnose→ask→apply loop into a single plan with one focused `AskUserQuestion` upfront. Don't fight the mode. *Why deferred:* skill modification — outside the safe set. Could also be argued as a project CLAUDE.md note, but the behaviour belongs *inside* the skill that needs to adapt, not in PiPanything's project conventions (the skill runs anywhere).
  - Source retro(s): `archive/2026-05-11-130929-retro-review-pipanything.md`

- **BACKLOG.md track for MCP overlay control (socket + CLI shim)** — file the architecture sketch from retro 3 as a new BACKLOG track so the next session that picks it up doesn't re-derive the design. *Diagnosis:* user explicitly offered "to write it up as a `BACKLOG.md` track next" and the session ended before it was filed. *Why deferred:* BACKLOG.md edits are out of the safe set (only project CLAUDE.md additions are auto-applicable, and this would be a brand-new BACKLOG entry — restructuring-adjacent, not a one-line append).
  - Source retro(s): `archive/2026-05-11-135003-mcp-overlay-control.md`
  - Suggested track shape:
    ```
    ### MCP overlay control (socket + CLI shim)

    Why: enable Claude (and other agents) to drive PiPanything — show a
    window/URL, set geometry, hide — while the user is heads-down in a
    fullscreen terminal, without embedding an MCP/HTTP server in the
    AppKit process.

    Architecture: GUI app exposes a Unix domain socket with a simple
    line/JSON protocol (open, set-geometry, hide, list-overlays, …). A
    separate `pipanythingctl` binary speaks stdio MCP and forwards every
    request to the socket. Same-user local IPC; auth is filesystem perms.

    Why this shape:
    - Stdio MCP servers are spawned per-session; the GUI is long-lived.
      The CLI shim is the natural seam.
    - Internal protocol stays decoupled from MCP spec churn — protocol
      versioning lives in the shim, not the app.
    - The CLI is independently useful as a scripting target (osascript,
      shell, hotkey).

    Out of scope: cross-machine, multi-user auth, sandboxing.

    Touches: new `Features/ControlSocket.swift` in the app, new
    `tools/pipanythingctl/` Swift package for the CLI/MCP shim,
    integration test that round-trips one command end-to-end.
    ```

- **context7 (and other MCP) availability for exploratory chats** — tooling/infra capability gap. Both MCP servers disconnected mid-session, forcing reasoning from prior knowledge instead of canonical MCP spec lookups. *Why deferred:* tooling/infrastructure changes are out of the safe set; the right surface is the user's MCP config, not project files.
  - Source retro(s): `archive/2026-05-11-135003-mcp-overlay-control.md`
  - Suggested action (for the user): pin context7 (and any other MCP servers used for spec lookups) into the project's default MCP set so they survive session restarts; alternatively, add a one-line note to the project workflow saying "WebFetch a known-good MCP spec URL as a fallback when context7 is down."

## Marked processed (no action)

- `archive/2026-05-11-134804-set-window-menu-action.md` — strong session, but the durable observations are already captured elsewhere: the `PIP_TEST_*` convention plus the headless-verification gap are both in `BACKLOG.md`'s "Headless verification toolkit" track (with the documented direction being *replacement* by a menu-driver helper, so reinforcing it in CLAUDE.md would conflict). The "CaptureManager already serializes stop()→start()" learning lives in the code itself. The `Set window…` `isCapturing` gating is a local decision in the PR. Nothing new to record durably.

## Processed: 2026-05-11

### Actions taken
- **Skill update** → `/Users/zolat/projects/claude-code-retros/retros/commands/retro.md`. Added `(Applied YYYY-MM-DD)` instruction as new step 1 of Step 5 — Mark processed and archive, so future reviewers see "already done" without re-deriving it. (Addresses deferred item 1.)
- **Skill update** → `/Users/zolat/projects/claude-code-retros/retros/commands/retro.md`. Added Plan-mode-override paragraph to the Output-style preamble: flatten iterative loop into one plan + one `AskUserQuestion`, exit plan mode, apply in batch. (Addresses deferred item 2.)
- **Skill update** → `/Users/zolat/projects/claude-code-retros/retros/commands/auto-retro.md`. Added "Items the user has explicitly signalled they'll handle themselves" to the defer list — captures the "don't pre-empt the user's framing" convention surfaced by the meta-retro on this run.

### Items discussed but not acted on
- **Item 3 — BACKLOG.md track for MCP overlay control** — no action; track already filed in `BACKLOG.md` lines 33 and 55-65 ("Agent-control surface (socket + CLI + MCP shim)") with a 2026-05-11 session note, full architecture sketch, phasing, and acceptance criteria. The auto-retro's drafted track was stale by the time it was written; Phase 1 implementation is already landing (`pipanythingctl` Rust binary, `Server/ControlServer.swift`, `PIP_CONTROL_SERVER=1`).
- **Item 4 — context7 MCP availability** — note, no edit. Transient MCP-server flakiness (context7 and Gmail both flapped in this session too). If it recurs, the right surface is project MCP settings, not retro-driven file edits.

## Notes

- Three retros, zero auto-applies — that's the right call here, not a failure mode. The strongest candidate for action (Retro 3's MCP-overlay-control architecture) belongs in `BACKLOG.md`, which the user already signalled they'd file next session; auto-writing it would pre-empt their framing. The other two retros surface skill/process gaps and a captured-elsewhere convention.
- Theme across retro 1 and recent archived meta-retros: the `/retros:retro` skill keeps surfacing the same capability gap — "did this queued edit already land?" Two adjacent suggestions across the archive now (the "Applied:" tag here, and an older one about telling already-applied vs never-tried unblocks apart). Worth a follow-up `/retro` cycle on the skill itself, not auto-actionable from here.
- The `PIP_TEST_*` convention came up again in retro 2. The repeated mention reinforces the BACKLOG-level decision to *replace* the per-feature env-var sprawl with a menu-driver helper — keep an eye on whether the next "menu-driven feature" session is the right moment to land that track instead of adding a fourth/fifth `PIP_TEST_*` hook.
